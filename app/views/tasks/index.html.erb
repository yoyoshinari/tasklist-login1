<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.content %></li>
  <% end %>
  <%= link_to '新規作成', tasks_path %>
  <%= link_to '一覧に戻る', tasks_path %>
</ul>